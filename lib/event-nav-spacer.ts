/** Bottom padding for any page rendering the floating EventNav, so content can scroll clear of it. Kept out of event-nav.tsx since that's a "use client" module and its exports can't be read as plain values from Server Components. */
export const EVENT_NAV_SPACER_CLASS = "pb-24";
